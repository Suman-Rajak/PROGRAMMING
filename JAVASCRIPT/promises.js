/**

        There are 2 conditions . Suceess or Fail.

        If we get the data => Everything allright
        If we dont get the data => Internet Error / Server Error

        
        // When we use "fetch()" method then it returns "promise".

        // In 90% case we consume promise, not create. We create promise in very advanced level.

        // We use promise to be protected from "callbackhell".


*/