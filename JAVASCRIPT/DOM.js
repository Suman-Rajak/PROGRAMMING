//Check Previous Visited Site
//window.history.back();
//HTML is the root element of the document


/* alert is used for a popup

    confirm is used for two options yes or cancel //if(confirm("String"))

*/

//DOM Navigation [ it incudes whitespaces as child ]
// -----------------------------------------------------

//To know the child nodes - document.name.childnodes()

//To know someone has child node or not - document.name.hasChildNodes()

//Acess First Child - document.name.firstElementChild (only firstChild includes whitespaces. But this does not include whitespaces.)

//To get sibling - document.name.nextsibling/previoussibling/nextelelementsibling/previouselementsibling





//Search element by reference

//**innerhtml is used to change the text part of html element */

//Get Element usind id - document.getElementById("ID") [similrly we can get elements using classname or tag or name (name attribute)]






//Queryselector(' # - ID,  . - Class')
/**it returns the first matching value */


//Queryselectorall()
/**it returns all matching elements */